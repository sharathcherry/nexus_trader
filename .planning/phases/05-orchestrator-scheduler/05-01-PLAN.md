---
phase: "05"
plan: "01"
type: tdd
wave: 1
slug: orchestrator-scheduler
date: "2026-06-06"
depends_on: []
files_modified:
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_config.py
  - tests/test_orchestrator.py
  - tests/test_scheduler.py
  - tests/test_main.py
  - tests/test_logger.py
  - pytest.ini
autonomous: true
requirements:
  - ORCH-01
  - ORCH-02
  - ORCH-03
  - ORCH-04
  - ORCH-05
  - ORCH-06
  - ORCH-07
test_command: "pytest tests/ -x -q"
must_haves:
  truths:
    - "18 tests are collected by pytest with zero import errors"
    - "All 18 tests fail with ImportError, AttributeError, or AssertionError — not SyntaxError"
    - "test_config.py covers is_trading_day for holiday, weekend, and normal weekday"
    - "test_orchestrator.py covers NexusTrader init, pre-market sequence, holiday guard, dry-run"
    - "test_scheduler.py covers executor type, job IDs, and OrTrigger for market_session"
    - "test_main.py covers banner text output, DRY-RUN mode line, parse_args flag, and shutdown sequence"
  artifacts:
    - path: "tests/__init__.py"
      provides: "makes tests/ a package"
    - path: "tests/conftest.py"
      provides: "shared mock fixtures: mock_agents, mock_portfolio"
    - path: "tests/test_config.py"
      provides: "3 tests for ORCH-04 (is_trading_day)"
    - path: "tests/test_orchestrator.py"
      provides: "4 tests for ORCH-01 (NexusTrader methods)"
    - path: "tests/test_scheduler.py"
      provides: "3 tests for ORCH-02 and ORCH-03 (TradingScheduler config)"
    - path: "tests/test_main.py"
      provides: "4 tests for ORCH-05, ORCH-06, ORCH-07 (main.py functions)"
    - path: "pytest.ini"
      provides: "testpaths = tests"
  key_links:
    - from: "tests/test_orchestrator.py"
      to: "execution.scheduler.NexusTrader"
      via: "import + patch"
      pattern: "from execution.scheduler import NexusTrader"
    - from: "tests/test_scheduler.py"
      to: "execution.scheduler.TradingScheduler"
      via: "import + MagicMock"
      pattern: "from execution.scheduler import TradingScheduler"
    - from: "tests/test_main.py"
      to: "main.print_banner, main.parse_args"
      via: "import"
      pattern: "from main import print_banner, parse_args"
    - from: "tests/test_config.py"
      to: "config.is_trading_day"
      via: "import singleton"
      pattern: "from config import config"
---

<objective>
Create the complete RED test scaffold for Phase 5. All 14 tests are syntactically valid pytest that FAIL because the implementation does not exist yet. Failures are expected ImportError or AttributeError from modules-under-test — not SyntaxError in the test files themselves.

Purpose: Establish the behavioral contracts that Wave 2 implementation must satisfy before a single line of production code is written.

Output: pytest.ini, tests/__init__.py, tests/conftest.py, tests/test_config.py, tests/test_orchestrator.py, tests/test_scheduler.py, tests/test_main.py — 14 tests total, all RED.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-orchestrator-scheduler/05-CONTEXT.md
@.planning/phases/05-orchestrator-scheduler/05-RESEARCH.md
@.planning/phases/05-orchestrator-scheduler/05-PATTERNS.md
@.planning/phases/05-orchestrator-scheduler/05-VALIDATION.md
</context>

<tasks>

<task type="tdd">
<name>Task 1: Create pytest infrastructure and conftest fixtures</name>
<files>pytest.ini, tests/__init__.py, tests/conftest.py</files>
<action>
Create the pytest configuration and shared fixture file. These are pure scaffold — no assertions, no failures, just infrastructure.

pytest.ini at the project root:
- Section header: [pytest]
- testpaths = tests
- addopts = -v (verbose output so RED failures show individual test names)

tests/__init__.py: empty file (same convention as agents/__init__.py and execution/__init__.py — single blank line). This makes tests/ a package so relative imports work.

tests/conftest.py: shared fixtures using unittest.mock.MagicMock and pytest.fixture. Create these fixtures:

1. mock_portfolio: a MagicMock() with spec-like attributes — force_squareoff_all, save_state, get_daily_report each set to MagicMock(). Used by test_orchestrator.py and test_main.py.

2. mock_agents: a dict of MagicMock() keyed by agent name ('i0', 'i1', 'i2', 'i3', 'i4', 'i6', 'i9'). Each value is a MagicMock() with a .run attribute that is also a MagicMock(). Used by test_orchestrator.py to assert call ordering.

Both fixtures are function-scoped (default) so each test gets a fresh mock.

No imports from production modules in conftest.py — conftest must not trigger ImportError during collection.
</action>
<verify>
pytest --collect-only -q 2>&1 | head -5 should show "no tests ran" cleanly (conftest loads without error, no test files yet fully collected if run in isolation, but no collection crash).
</verify>
<done>pytest.ini exists at project root with testpaths=tests. tests/__init__.py exists as empty file. tests/conftest.py exists with mock_portfolio and mock_agents fixtures importable without error.</done>
</task>

<task type="tdd">
<name>Task 2: Write test_config.py — 3 RED tests for ORCH-04 (is_trading_day)</name>
<files>tests/test_config.py</files>
<action>
Write three tests that import from config and assert behaviors of is_trading_day() and NSE_HOLIDAYS_2026. These will FAIL in Wave 1 because is_trading_day() does not exist on the Config class yet (config.py currently has no is_trading_day method and no NSE_HOLIDAYS_2026 attribute).

File header:
```
"""Tests for config.is_trading_day() and NSE_HOLIDAYS_2026 — ORCH-04."""
from datetime import date
from config import config
```

Test 1 — test_is_trading_day_holiday:
- d = date(2026, 1, 26) — Republic Day (Monday, verified weekday holiday)
- assert config.is_trading_day(d) is False
- Failure reason in Wave 1: AttributeError — 'Config' object has no attribute 'is_trading_day'

Test 2 — test_is_trading_day_weekend:
- d = date(2026, 6, 6) — Saturday (today's date per context; confirmed weekend)
- assert config.is_trading_day(d) is False
- Failure reason in Wave 1: same AttributeError

Test 3 — test_is_trading_day_weekday:
- d = date(2026, 6, 8) — Monday, not in NSE_HOLIDAYS_2026 (confirmed from RESEARCH.md holiday list)
- assert config.is_trading_day(d) is True
- Failure reason in Wave 1: same AttributeError

No parametrize, no fixtures needed — three plain test functions.
</action>
<verify>
pytest tests/test_config.py -v 2>&1 should show 3 tests FAILED (not ERROR from syntax, not skipped). Failure message should contain "AttributeError" for all three.
</verify>
<done>tests/test_config.py exists with 3 syntactically valid test functions. pytest collects all 3. All 3 fail with AttributeError in Wave 1 RED state.</done>
</task>

<task type="tdd">
<name>Task 3: Write test_orchestrator.py — 4 RED tests for ORCH-01</name>
<files>tests/test_orchestrator.py</files>
<action>
Write four tests that import NexusTrader from execution.scheduler and assert constructor and method behaviors. These will FAIL in Wave 1 with ImportError because execution/scheduler.py does not exist yet.

File header:
```
"""Tests for NexusTrader orchestration methods — ORCH-01."""
from unittest.mock import MagicMock, patch, call
import pytest
```

Import is done inside each test (or at module level — use module-level, accept the ImportError as the RED failure mode):
```
from execution.scheduler import NexusTrader
```

Test 1 — test_nexus_trader_init:
- Patch all 7 agent classes and PaperPortfolio in 'execution.scheduler' namespace
- Instantiate NexusTrader(dry_run=False)
- Assert trader._portfolio is not None
- Assert trader._agent_i0 is not None (spot-check; full init verified by absence of AttributeError)
- Assert trader.dry_run is False

Test 2 — test_pre_market_pipeline_sequence:
- Patch AgentI0, AgentI1, AgentI2, AgentI3, PaperPortfolio in execution.scheduler
- Patch config.is_trading_day to return True (so holiday guard does not fire)
- Instantiate NexusTrader(dry_run=False)
- Call trader.run_pre_market_pipeline()
- Assert MockI0.return_value.run.called is True
- Assert MockI1.return_value.run.called is True
- Assert MockI2.return_value.run.called is True
- Assert MockI3.return_value.run.called is True
- Assert call order: I0.run called before I1.run before I2.run before I3.run
  (use assert MockI0.return_value.run.call_count == 1 at minimum; ordering via mock_calls on a manager mock is optional for Wave 1 — the assert_called_once checks are sufficient)

Test 3 — test_pre_market_holiday_guard:
- Patch PaperPortfolio and all agents in execution.scheduler
- Patch 'execution.scheduler.config.is_trading_day' to return False (holiday)
- Instantiate NexusTrader(dry_run=False)
- Call trader.run_pre_market_pipeline()
- Assert MockI0.return_value.run.called is False — agents must NOT be called on a holiday

Test 4 — test_pre_market_prints_watchlist (ORCH-02):
- Patch all agents and PaperPortfolio in execution.scheduler
- Patch config.is_trading_day to return True
- Configure MockI3.return_value.run.return_value = [{'symbol': 'RELIANCE.NS', 'gap_pct': 2.1, 'strategy': 'gap_up', 'rr_ratio': 2.0, 'catalyst': 'breakout'}]
- Instantiate NexusTrader(dry_run=False)
- Call trader.run_pre_market_pipeline() with capsys fixture
- captured = capsys.readouterr()
- assert 'RELIANCE.NS' in captured.out — watchlist table must appear in stdout
- assert 'Symbol' in captured.out or 'symbol' in captured.out.lower() — table header present
- Failure reason in Wave 1: ImportError

Test 5 — test_market_session_dry_run:
- Patch AgentI4, AgentI6, PaperPortfolio in execution.scheduler
- Instantiate NexusTrader(dry_run=True)
- Call trader.run_market_session()
- Assert MockI6.return_value.run.called is False — AgentI6 (order placement) must NOT run in dry_run mode
- Assert trader.dry_run is True

All four tests use `with patch(...)` context managers. The full list of patches for tests that require all agents is: 'execution.scheduler.AgentI0', 'execution.scheduler.AgentI1', 'execution.scheduler.AgentI2', 'execution.scheduler.AgentI3', 'execution.scheduler.AgentI4', 'execution.scheduler.AgentI6', 'execution.scheduler.AgentI9', 'execution.scheduler.PaperPortfolio'.
</action>
<verify>
pytest tests/test_orchestrator.py -v 2>&1 should show 4 tests FAILED with ImportError or ModuleNotFoundError for 'execution.scheduler'. No SyntaxError.
</verify>
<done>tests/test_orchestrator.py exists with 4 syntactically valid test functions. pytest collects all 4. All 4 fail with ImportError in Wave 1 RED state.</done>
</task>

<task type="tdd">
<name>Task 4: Write test_scheduler.py — 3 RED tests for ORCH-02 and ORCH-03</name>
<files>tests/test_scheduler.py</files>
<action>
Write three tests that inspect TradingScheduler configuration WITHOUT calling scheduler.start(). These will FAIL in Wave 1 with ImportError because execution/scheduler.py does not exist yet.

File header:
```
"""Tests for TradingScheduler APScheduler configuration — ORCH-02, ORCH-03."""
from unittest.mock import MagicMock
from apscheduler.executors.pool import ThreadPoolExecutor as APSThreadPoolExecutor
from apscheduler.triggers.combining import OrTrigger
from execution.scheduler import TradingScheduler
```

Test 1 — test_scheduler_executor_type:
- Create trader = MagicMock() (avoids needing real NexusTrader)
- ts = TradingScheduler(nexus_trader=trader)
- Assert ts._scheduler is not None
- Assert isinstance(ts._scheduler._executors['default'], APSThreadPoolExecutor)
- Also assert ts._scheduler._executors['default']._max_workers == 1
- Do NOT call ts._scheduler.start()

Test 2 — test_scheduler_job_ids:
- Create trader = MagicMock()
- ts = TradingScheduler(nexus_trader=trader)
- job_ids = {job.id for job in ts._scheduler.get_jobs()}
- assert job_ids == {'pre_market', 'market_session', 'post_market'}
- Do NOT call ts._scheduler.start()

Test 3 — test_market_trigger_type:
- Create trader = MagicMock()
- ts = TradingScheduler(nexus_trader=trader)
- market_job = next(job for job in ts._scheduler.get_jobs() if job.id == 'market_session')
- assert isinstance(market_job.trigger, OrTrigger)
- Also assert market_job.max_instances == 1
- Do NOT call ts._scheduler.start()

Note: APScheduler's _executors attribute and get_jobs() are available before start() is called — jobs are stored in the jobstore as soon as add_job() is called on an unstarted scheduler. This is verified in RESEARCH.md.
</action>
<verify>
pytest tests/test_scheduler.py -v 2>&1 should show 3 tests FAILED with ImportError for 'execution.scheduler'. No SyntaxError.
</verify>
<done>tests/test_scheduler.py exists with 3 syntactically valid test functions. pytest collects all 3. All 3 fail with ImportError in Wave 1 RED state.</done>
</task>

<task type="tdd">
<name>Task 5: Write test_main.py — 4 RED tests for ORCH-05, ORCH-06, ORCH-07</name>
<files>tests/test_main.py</files>
<action>
Write four tests that import from main.py and assert banner output, argparse behavior, and shutdown sequence. These will FAIL in Wave 1 with ImportError or AttributeError because main.py currently only has a stub main() and no print_banner() or parse_args() functions.

File header:
```
"""Tests for main.py entry point — ORCH-05 (shutdown), ORCH-06 (banner), ORCH-07 (argparse)."""
import sys
from unittest.mock import MagicMock, patch, call
import pytest
from main import print_banner, parse_args
```

Test 1 — test_banner_output:
- Call print_banner(dry_run=False) using capsys fixture
- captured = capsys.readouterr()
- assert 'NEXUS' in captured.out or 'nexus' in captured.out.lower() — banner must contain the project name in some form
- assert 'TRADER' in captured.out or 'trader' in captured.out.lower()
- assert 'LIVE' in captured.out — info block shows mode=LIVE when dry_run=False
- Failure reason in Wave 1: ImportError — cannot import name 'print_banner' from 'main'

Test 2 — test_banner_dry_run_mode:
- Call print_banner(dry_run=True) using capsys fixture
- captured = capsys.readouterr()
- assert 'DRY-RUN' in captured.out — info block must show DRY-RUN when dry_run=True
- assert 'LIVE' not in captured.out — must NOT show LIVE when dry_run is True
- Failure reason in Wave 1: ImportError

Test 3 — test_parse_args_dry_run:
- args_default = parse_args([]) — pass empty list to avoid reading sys.argv in test
  Note: parse_args must accept an optional args parameter or the test must patch sys.argv.
  Use: with patch('sys.argv', ['main']): args = parse_args()
  Then with patch('sys.argv', ['main', '--dry-run']): args2 = parse_args()
- assert args.dry_run is False
- assert args2.dry_run is True
- Failure reason in Wave 1: ImportError

Test 4 — test_keyboard_interrupt_shutdown:
- Mock NexusTrader, TradingScheduler, and shutdown_event from main module
- Patch 'main.NexusTrader' and 'main.TradingScheduler' and 'main.shutdown_event'
- Also patch sys.exit to avoid actually exiting
- Simulate the KeyboardInterrupt path:
  The test calls the KeyboardInterrupt handler block directly by patching shutdown_event.wait to raise KeyboardInterrupt
  Use patch('main.shutdown_event') with mock_event.wait.side_effect = KeyboardInterrupt
  Then call main.main() with parse_args patched to return a Namespace(dry_run=False)
  And print_banner patched to a no-op
- Assert mock_trader._portfolio.force_squareoff_all.called is True
- Assert mock_trader._portfolio.save_state.called is True
- Assert mock_scheduler.shutdown.called is True
- Assert sys.exit called with 0
- Alternatively: if the direct main() call approach is complex, test the shutdown sequence more directly:
  Instantiate the sequence manually — create a mock trader with _portfolio mock, call the 3 shutdown steps explicitly, assert their call order via mock_manager.mock_calls. This simpler form is acceptable for Wave 1 RED state as long as the assertions exist and the import still fails.
- Failure reason in Wave 1: ImportError — cannot import name 'print_banner' from 'main'

All tests must be syntactically valid Python. Accept that the exact shutdown test implementation may need minor adjustment in Wave 2 once the real main() signature is known.
</action>
<verify>
pytest tests/test_main.py -v 2>&1 should show 4 tests FAILED with ImportError for 'print_banner' or 'parse_args'. No SyntaxError.
</verify>
<done>tests/test_main.py exists with 4 syntactically valid test functions. pytest collects all 4. All 4 fail with ImportError in Wave 1 RED state.</done>
</task>

<task type="tdd">
<name>Task 6: Write test_logger.py — 3 RED tests for ORCH-06 (custom log levels)</name>
<files>tests/test_logger.py</files>
<action>
Write three tests that assert TRADE, PNL_PROFIT, and PNL_LOSS custom levels exist on the logger after setup_logger() is called. These will FAIL in Wave 1 because the custom levels are not yet registered in utils/logger.py.

File header:
```
"""Tests for custom TRADE/P&L log levels in utils.logger — ORCH-06."""
import logging
from utils.logger import setup_logger
```

Test 1 — test_trade_level_registered:
- assert logging.getLevelName('TRADE') == 25
- Failure in Wave 1: AssertionError — getLevelName returns 'Level 25' (not registered yet)

Test 2 — test_pnl_profit_level_registered:
- assert logging.getLevelName('PNL_PROFIT') == 26
- Failure in Wave 1: AssertionError

Test 3 — test_trade_method_callable:
- logger = setup_logger('test_trade')
- assert hasattr(logger, 'trade') — method must be bound on Logger instances
- assert hasattr(logger, 'pnl_profit')
- assert hasattr(logger, 'pnl_loss')
- Failure in Wave 1: AssertionError (methods not yet bound on logging.Logger)

No fixtures needed — three plain test functions.
</action>
<verify>
pytest tests/test_logger.py -v 2>&1 should show 3 tests FAILED with AssertionError. No SyntaxError or ImportError.
</verify>
<done>tests/test_logger.py exists with 3 syntactically valid test functions. pytest collects all 3. All 3 fail with AssertionError in Wave 1 RED state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → production modules | Tests import production modules; mocks prevent side effects (no real API calls, no real file I/O) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | conftest.py shared fixtures | accept | MagicMock fixtures are reset per function scope; no shared mutable state between tests |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this wave — all test dependencies (pytest, unittest.mock) are stdlib or already pinned in requirements.txt |
</threat_model>

<verification>
After completing all tasks in this wave:

```
pytest tests/ -x -q
```

Expected outcome: 18 tests COLLECTED. Most or all FAIL. Zero ERRORS (no SyntaxError in test files). Zero collection crashes.

Acceptable failure modes per test file:
- test_config.py: AttributeError — 'Config' object has no attribute 'is_trading_day'
- test_orchestrator.py: ModuleNotFoundError — No module named 'execution.scheduler'
- test_scheduler.py: ModuleNotFoundError — No module named 'execution.scheduler'
- test_main.py: ImportError — cannot import name 'print_banner' from 'main'
- test_logger.py: AssertionError — TRADE/PNL levels not yet registered

NOT acceptable: SyntaxError, IndentationError, or test collection crash (E prefix in pytest output).
</verification>

<success_criteria>
Wave 1 is complete when:
- pytest.ini exists at project root with testpaths = tests
- tests/__init__.py exists (empty)
- tests/conftest.py exists with mock_portfolio and mock_agents fixtures
- tests/test_config.py: 3 tests collected, 3 FAIL (AttributeError)
- tests/test_orchestrator.py: 5 tests collected, 5 FAIL (ImportError) — includes watchlist print test
- tests/test_scheduler.py: 3 tests collected, 3 FAIL (ImportError)
- tests/test_main.py: 4 tests collected, 4 FAIL (ImportError)
- tests/test_logger.py: 3 tests collected, 3 FAIL (AssertionError — levels not registered yet)
- Total: 18 collected, 18 FAILED, 0 ERROR, 0 passed
- All failures are import/attribute/assertion failures, NOT syntax failures
</success_criteria>

<output>
Create `.planning/phases/05-orchestrator-scheduler/05-01-SUMMARY.md` when done.
</output>
