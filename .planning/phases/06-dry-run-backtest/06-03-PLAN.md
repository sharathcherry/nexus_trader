---
phase: 06-dry-run-backtest
plan: 03
type: execute
wave: 2
depends_on:
  - 06-02
files_modified:
  - backtest.py
  - README.md
autonomous: true
requirements:
  - TEST-06
  - TEST-07

must_haves:
  truths:
    - "python backtest.py --start 2025-01-01 --end 2025-03-31 runs without raising and prints a tabulate report"
    - "Backtest report contains two tables: summary metrics and monthly returns, both using tablefmt='rounded_outline'"
    - "Results saved to logs/backtest/backtest_YYYYMMDD_HHmmss.json; directory created if absent"
    - "README.md exists at project root with all 5 required sections and 3 run-mode code blocks"
    - "--start and --end are required args; --capital is optional; invalid date format exits with code 1"
  artifacts:
    - path: "backtest.py"
      provides: "CLI entry point for NexusBacktester — argparse, date validation, report printing, JSON save"
      contains: "NexusBacktester"
    - path: "README.md"
      provides: "Developer-facing documentation: Overview, Prerequisites, .env Setup, Run Modes, Project Structure"
      contains: "python main.py --dry-run"
  key_links:
    - from: "backtest.py main()"
      to: "execution.backtester.NexusBacktester"
      via: "from execution.backtester import NexusBacktester"
      pattern: "from execution\\.backtester import NexusBacktester"
    - from: "backtest.py _save_results()"
      to: "logs/backtest/"
      via: "Path('logs/backtest').mkdir(parents=True, exist_ok=True)"
      pattern: "logs.*backtest"
---

<objective>
Implement backtest.py (project root entry point) and write README.md (project root documentation).

backtest.py wires the NexusBacktester from plan 06-02 to a CLI: argparse --start/--end/--capital, date validation, tabulate terminal report (two tables), JSON save to logs/backtest/.

README.md documents the project for a developer returning after a break: installation, .env key sources, all three run modes.

Purpose: Completes the user-facing backtest workflow (TEST-06) and project documentation (TEST-07).

Output: backtest.py at project root, README.md at project root.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-dry-run-backtest/06-CONTEXT.md
@.planning/phases/06-dry-run-backtest/06-PATTERNS.md
@.planning/phases/06-dry-run-backtest/06-02-SUMMARY.md

<interfaces>
<!-- NexusBacktester contract from plan 06-02 (must be complete before this plan runs). -->

NexusBacktester constructor:
  NexusBacktester(start_date: str, end_date: str, capital: float | None = None)
  from execution.backtester import NexusBacktester

NexusBacktester.run() returns dict with keys:
  total_trades: int
  win_rate: float              (0.0–1.0)
  total_net_pnl: float
  total_return_pct: float      (decimal, e.g. 0.12 for 12%)
  sharpe_ratio: float
  max_drawdown_pct: float      (decimal, e.g. 0.15 for 15%)
  profit_factor: float
  monthly_returns: list[{'month': 'YYYY-MM', 'pnl': float, 'trades': int}]
  trading_days_processed: int  (bonus key — save to JSON, do NOT print in terminal report)

config.CAPITAL: float  (default starting capital, e.g. 100000)

tabulate report format (per D-13):
  - Table 1: summary metrics — 7 rows (exclude trading_days_processed from terminal output)
  - Table 2: monthly returns — one row per month
  - Both tables: tablefmt='rounded_outline'

JSON save path (per D-14):
  logs/backtest/backtest_YYYYMMDD_HHmmss.json
  Payload: {**results, 'start_date': str, 'end_date': str, 'capital': float}
  (trading_days_processed IS included in JSON — it is in results dict)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backtest.py — argparse, date validation, NexusBacktester invocation, tabulate report, JSON save</name>
  <files>backtest.py</files>

  <read_first>
  - .planning/phases/06-dry-run-backtest/06-PATTERNS.md — §"backtest.py" for complete argparse pattern, validate_dates(), print_banner(), _print_report(), _save_results(), and main() body
  - .planning/phases/06-dry-run-backtest/06-CONTEXT.md — D-13 (tabulate format), D-14 (JSON save path), D-16 (argparse spec), D-17 (import pattern)
  - main.py — observe the existing entry-point structure (argparse, if __name__ == '__main__', sys.stdout.reconfigure pattern) to keep consistency
  </read_first>

  <action>
  Create backtest.py at the project root as a new file.

  Imports: from __future__ import annotations; import argparse, json, sys; from datetime import datetime; from pathlib import Path; from config import config; from execution.backtester import NexusBacktester; from utils.logger import setup_logger. Module-level: logger = setup_logger(__name__).

  parse_args() -> argparse.Namespace: ArgumentParser description='nexus_trader — NSE intraday paper trading backtester'. Three arguments: --start (required=True, metavar='YYYY-MM-DD'), --end (required=True, metavar='YYYY-MM-DD'), --capital (type=float, default=None, help includes "default: config.CAPITAL"). Return parser.parse_args().

  validate_dates(start_str: str, end_str: str) -> tuple[datetime, datetime]: parse with datetime.strptime(x, '%Y-%m-%d'); on ValueError print error message and sys.exit(1). Check start <= end; if not print message and sys.exit(1). Return (start, end).

  print_banner() -> None: print three lines — blank line, '  nexus_trader — Backtest Engine', '  Rule-based daily OHLC simulation | NSE Nifty 100', blank line. Use plain print(), not colorlog.

  _print_report(results: dict) -> None: import tabulate from tabulate inside function. Build summary_rows with 7 rows (Total Trades, Win Rate as f"{v:.1%}", Total Net P&L as f"Rs. {v:,.2f}", Total Return as f"{v:.2%}", Sharpe Ratio as f"{v:.3f}", Max Drawdown as f"{v:.2%}", Profit Factor as f"{v:.2f}"). Print tabulate(summary_rows, headers=['Metric', 'Value'], tablefmt='rounded_outline'). Build monthly_rows from results['monthly_returns'] (each row: month, trades, f"Rs. {pnl:,.2f}"). If monthly_rows is non-empty, print second table with headers=['Month', 'Trades', 'Net P&L'] and tablefmt='rounded_outline'.

  _save_results(results: dict, start_str: str, end_str: str, capital: float) -> None: Path('logs/backtest').mkdir(parents=True, exist_ok=True). ts = datetime.now().strftime('%Y%m%d_%H%M%S'). filepath = Path('logs/backtest') / f'backtest_{ts}.json'. payload = {**results, 'start_date': start_str, 'end_date': end_str, 'capital': capital}. Write with json.dump(payload, f, indent=2, default=str). logger.info('Results saved to %s', filepath).

  main() -> None: validate sys.stdout encoding. args = parse_args(). print_banner(). validate_dates(args.start, args.end) (call for side-effect validation; discard returned datetimes if not needed downstream). capital = args.capital if args.capital is not None else config.CAPITAL. bt = NexusBacktester(start_date=args.start, end_date=args.end, capital=capital). logger.info('Running backtest %s → %s, capital=%.0f', args.start, args.end, capital). results = bt.run(). _print_report(results). _save_results(results, args.start, args.end, capital).

  if __name__ == '__main__': main()
  </action>

  <verify>
  <automated>python -c "import ast; ast.parse(open('backtest.py').read()); print('Syntax OK')"</automated>
  </verify>

  <acceptance_criteria>
  - backtest.py parses without SyntaxError
  - python backtest.py --help shows --start, --end, --capital in output (exits 0)
  - python backtest.py exits with non-zero code when --start or --end is omitted (required arg)
  - python backtest.py --start 2025-01-01 --end 2024-01-01 exits with code 1 and prints a "must be" error message (start > end validation)
  - python backtest.py --start bad-date --end 2025-03-31 exits with code 1 (invalid date format)
  - backtest.py contains "from execution.backtester import NexusBacktester" — no scheduler, no agent imports
  - backtest.py contains "tablefmt='rounded_outline'" — correct tabulate format
  - backtest.py contains "logs/backtest" and "mkdir(parents=True, exist_ok=True)"
  - backtest.py contains "if __name__ == '__main__':" entry guard
  - grep -c "TradingScheduler\|NexusTrader\|APScheduler" backtest.py returns 0 (no scheduler imports)
  </acceptance_criteria>

  <done>backtest.py created; syntax valid; --help works; date validation exits correctly; no scheduler imports present</done>
</task>

<task type="auto">
  <name>Task 2: Write README.md at project root — 5 sections, 3 run modes, 4 .env keys</name>
  <files>README.md</files>

  <read_first>
  - .planning/phases/06-dry-run-backtest/06-CONTEXT.md — D-18, D-19 (README spec: sections, tone, 4 env keys, 3 run modes)
  - .planning/phases/06-dry-run-backtest/06-PATTERNS.md — §"README.md" for the 4 required .env keys and their sources, the 3 run-mode code blocks, and tone guidance
  - config.py — read lines 1–20 to confirm the actual env var names the code requires (GEMINI_API_KEY, ANTHROPIC_API_KEY, TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID)
  </read_first>

  <action>
  Create README.md at the project root.

  Section 1 "Overview": 3-4 sentences. Describe nexus_trader as a fully automated NSE India intraday paper trading system. Key facts: Nifty 100 gap scanner, Gemini Flash for pre-market ranking, Claude Sonnet for post-market review, zero real money, Zerodha-style brokerage math. Starting capital: Rs 1,00,000 paper capital.

  Section 2 "Prerequisites & Installation": Python 3.11+ required. Single code block: pip install -r requirements.txt. Note that google-genai>=2.0.0 (not google-generativeai) is used.

  Section 3 ".env Setup": Explain that a .env file must be created at the project root (copy .env.example). List all four keys as a code block showing KEY=value format with placeholder values. For each key, one line of prose describing the source URL:
  - GEMINI_API_KEY — Google AI Studio: https://aistudio.google.com/app/apikey
  - ANTHROPIC_API_KEY — Anthropic Console: https://console.anthropic.com/settings/keys
  - TELEGRAM_BOT_TOKEN — BotFather on Telegram (create bot with /newbot command)
  - TELEGRAM_CHAT_ID — your Telegram user or group chat ID (use @userinfobot to find it)

  Section 4 "Run Modes": Exactly three fenced code blocks with prose before each:
  1. Live trading mode (runs full daily pipeline with APScheduler, Mon-Fri IST):
     python main.py
  2. Dry-run mode (one-shot pre-market pipeline on yesterday's data, exits without starting scheduler):
     python main.py --dry-run
  3. Backtest mode (rule-based replay over a date range, prints report, saves JSON):
     python backtest.py --start YYYY-MM-DD --end YYYY-MM-DD [--capital FLOAT]

  Section 5 "Project Structure": A brief file-tree block showing the top-level layout. Include: main.py, backtest.py, config.py, requirements.txt, .env.example, agents/ (pre-market, market session, post-market agents), data/ (NSE universe, market data, indicators), execution/ (scheduler, portfolio, backtester), utils/ (logger), logs/ (auto-created), .planning/ (GSD planning artifacts).

  Tone per D-19: practical, no marketing language, target reader is the developer returning after a break. No architecture diagrams. No emoji. Keep total length under 120 lines.
  </action>

  <verify>
  <automated>python -c "
content = open('README.md', encoding='utf-8').read()
checks = [
    ('Overview section', '## Overview' in content or '# nexus_trader' in content),
    ('Install section',  'pip install -r requirements.txt' in content),
    ('env section',      'GEMINI_API_KEY' in content and 'ANTHROPIC_API_KEY' in content),
    ('Telegram keys',    'TELEGRAM_BOT_TOKEN' in content and 'TELEGRAM_CHAT_ID' in content),
    ('Live run mode',    'python main.py' in content),
    ('Dry-run mode',     'python main.py --dry-run' in content),
    ('Backtest mode',    'python backtest.py --start' in content),
    ('3 code fences',    content.count('```') >= 6),
]
failed = [name for name, result in checks if not result]
assert not failed, f'README missing: {failed}'
print('OK: README.md has all required content')
"</automated>
  </verify>

  <acceptance_criteria>
  - README.md exists at project root
  - Contains "pip install -r requirements.txt" in a fenced code block
  - Contains all 4 env keys: GEMINI_API_KEY, ANTHROPIC_API_KEY, TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID
  - Contains source URLs for GEMINI_API_KEY (aistudio.google.com) and ANTHROPIC_API_KEY (console.anthropic.com)
  - Contains exactly 3 run-mode code blocks: "python main.py", "python main.py --dry-run", "python backtest.py --start"
  - Has at least 5 section headings (##)
  - Does not mention "google-generativeai" as the SDK to install (must say google-genai)
  - Verification script above passes with "OK: README.md has all required content"
  - File length <= 150 lines (practical, not exhaustive)
  </acceptance_criteria>

  <done>README.md written with 5 sections; all 4 env keys documented with sources; 3 run modes shown; verification script passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → backtest.py | --start/--end are user-controlled strings; validated with strptime before use |
| backtest.py → filesystem | logs/backtest/ write; no path traversal risk (path is hardcoded, not user-controlled) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Tampering | --start/--end date args | mitigate | datetime.strptime validation in validate_dates(); sys.exit(1) on parse failure |
| T-06-07 | Information Disclosure | JSON save to logs/ | accept | logs/ is local filesystem, no sensitive data (all paper trading data) |
| T-06-08 | Tampering | README API key sources | accept | README documents URLs only — no actual keys committed; .env listed in .gitignore |
| T-06-SC | Tampering | pip installs | accept | No new packages — tabulate, json, pathlib already in requirements.txt/stdlib |
</threat_model>

<verification>
- python -c "import ast; ast.parse(open('backtest.py').read()); print('OK')" passes
- python backtest.py --help shows --start, --end, --capital
- python backtest.py --start bad --end 2025-03-31 exits with code 1
- README.md verification script (Task 2 verify block) passes
- grep "TradingScheduler\|NexusTrader" backtest.py returns no matches
- README.md mentions google-genai (not google-generativeai) as the install target
</verification>

<success_criteria>
- backtest.py: --start/--end required, --capital optional; date validation exits 1 on bad input; NexusBacktester.run() called; tabulate report with tablefmt='rounded_outline'; JSON saved to logs/backtest/backtest_YYYYMMDD_HHmmss.json
- README.md: 5 sections; all 4 .env keys with source URLs; 3 run-mode code blocks; under 150 lines; practical tone
- All 7 TEST requirement IDs (TEST-01 through TEST-07) covered across plans 06-01, 06-02, 06-03
</success_criteria>

<output>
Create `.planning/phases/06-dry-run-backtest/06-03-SUMMARY.md` when done
</output>
