---
phase: 06-dry-run-backtest
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - main.py
  - execution/scheduler.py
autonomous: true
requirements:
  - TEST-01
  - TEST-02

must_haves:
  truths:
    - "python main.py --dry-run runs run_pre_market_pipeline() on yesterday's date and exits with code 0"
    - "python main.py --dry-run never starts TradingScheduler"
    - "python main.py (no flag) still starts TradingScheduler normally"
    - "run_pre_market_pipeline() accepts an optional date_override parameter"
  artifacts:
    - path: "main.py"
      provides: "dry-run one-shot branch — runs pre-market pipeline then sys.exit(0)"
      contains: "if args.dry_run"
    - path: "execution/scheduler.py"
      provides: "run_pre_market_pipeline() with optional date_override parameter"
      contains: "date_override"
  key_links:
    - from: "main.py dry-run branch"
      to: "execution/scheduler.NexusTrader.run_pre_market_pipeline()"
      via: "date_override=yesterday parameter"
      pattern: "run_pre_market_pipeline.*date_override"
---

<objective>
Modify main.py to implement --dry-run as a one-shot mode: when the flag is passed, the full pre-market pipeline runs on yesterday's date, prints the watchlist table, then exits without starting TradingScheduler.

Purpose: Enables pre-live validation of agents and data fetching without spinning up the full scheduling loop. Satisfies TEST-01 and TEST-02.

Output: Modified main.py (dry-run branch), modified execution/scheduler.py (date_override parameter on run_pre_market_pipeline).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-dry-run-backtest/06-CONTEXT.md
@.planning/phases/06-dry-run-backtest/06-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Derived from Phase 5 PATTERNS.md. -->

From .planning/phases/05-orchestrator-scheduler/05-PATTERNS.md (Phase 5 spec for main.py):

main.py imports (Phase 5 spec):
  from __future__ import annotations
  import argparse, sys, threading
  from config import config
  from execution.scheduler import NexusTrader, TradingScheduler
  from utils.logger import setup_logger
  logger = setup_logger(__name__)
  shutdown_event = threading.Event()

parse_args() signature (Phase 5 — unchanged by Phase 6):
  def parse_args() -> argparse.Namespace:
      # --dry-run: action='store_true', default=False

NexusTrader.run_pre_market_pipeline() (Phase 5 current signature — no date_override):
  def run_pre_market_pipeline(self) -> None:
      # internally calls datetime.now(ist).date() for holiday check

Phase 6 modification (per D-03 from CONTEXT.md):
  - NexusTrader(dry_run=False)  # always False; Phase 6 replaces dry_run semantics
  - run_pre_market_pipeline(date_override=yesterday)  # inject yesterday's date
  - sys.exit(0) after pipeline completes in dry-run mode
  - TradingScheduler NEVER instantiated in dry-run mode

print_banner(dry_run=True) still displays "DRY-RUN" label (Phase 5 behavior preserved).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add date_override parameter to run_pre_market_pipeline()</name>
  <files>execution/scheduler.py</files>

  <read_first>
  - execution/scheduler.py — current implementation; find run_pre_market_pipeline() and all internal datetime.now(ist).date() calls
  - .planning/phases/05-orchestrator-scheduler/05-PATTERNS.md — NexusTrader public method patterns, is_trading_day usage
  - .planning/phases/06-dry-run-backtest/06-CONTEXT.md — D-01, D-02, D-03 for exact intent
  </read_first>

  <action>
  In execution/scheduler.py, modify run_pre_market_pipeline() to accept an optional parameter: date_override: date | None = None (import date from datetime at the top of the file if not already imported).

  Inside run_pre_market_pipeline(), replace the call to datetime.now(ist).date() that determines which date to process with: today = date_override if date_override is not None else datetime.now(ist).date(). Apply this substitution wherever the method uses the current date for the holiday check (config.is_trading_day(today)) and for the gap screening date passed to AgentI1. Do not change any other method signatures.

  Add the required import at module top if missing: from datetime import date, datetime, timedelta.

  The method signature becomes: def run_pre_market_pipeline(self, date_override: date | None = None) -> None.

  All existing callers that pass no argument continue to work unchanged (default None falls back to datetime.now).
  </action>

  <verify>
  <automated>python -c "from execution.scheduler import NexusTrader; import inspect; sig = inspect.signature(NexusTrader.run_pre_market_pipeline); assert 'date_override' in sig.parameters, 'date_override param missing'; print('OK: date_override present')"</automated>
  </verify>

  <acceptance_criteria>
  - execution/scheduler.py contains def run_pre_market_pipeline(self, date_override: date | None = None)
  - The line `today = date_override if date_override is not None else datetime.now(ist).date()` (or equivalent None-check pattern) is present in the method body
  - No other public method signatures changed
  - `python -c "from execution.scheduler import NexusTrader"` exits without ImportError
  </acceptance_criteria>

  <done>run_pre_market_pipeline() accepts date_override; existing callers unaffected; import check passes</done>
</task>

<task type="auto">
  <name>Task 2: Implement --dry-run one-shot branch in main.py</name>
  <files>main.py</files>

  <read_first>
  - main.py — current stub (lines 1–13); understand what exists before modifying
  - .planning/phases/06-dry-run-backtest/06-PATTERNS.md — §"main.py" section with complete Phase 5 + Phase 6 patterns, imports list, exact dry-run branch code
  - .planning/phases/06-dry-run-backtest/06-CONTEXT.md — D-01, D-02, D-03 (exact behavior spec)
  - .planning/phases/05-orchestrator-scheduler/05-PATTERNS.md — Phase 5 main() structure for the non-dry-run path
  </read_first>

  <action>
  Rewrite main.py to implement both the normal live path (Phase 5 spec) and the Phase 6 dry-run one-shot branch.

  Imports (complete set): from __future__ import annotations; import argparse, sys, threading, pytz; from datetime import datetime, timedelta; from config import config; from execution.scheduler import NexusTrader, TradingScheduler; from utils.logger import setup_logger.

  Module-level: logger = setup_logger(__name__) and shutdown_event = threading.Event().

  parse_args() — keep Phase 5 spec verbatim: --dry-run action='store_true' default=False. No new arguments added (backtest args live in backtest.py per D-16).

  print_banner(dry_run: bool = False) — keep Phase 5 spec; print_banner(dry_run=True) shows "DRY-RUN" label.

  main() body — per D-03:
  1. sys.stdout.reconfigure(encoding='utf-8') if hasattr(sys.stdout, 'reconfigure')
  2. args = parse_args()
  3. print_banner(dry_run=args.dry_run)
  4. If args.dry_run: ist = pytz.timezone('Asia/Kolkata'); yesterday = datetime.now(ist).date() - timedelta(days=1); trader = NexusTrader(dry_run=False); trader.run_pre_market_pipeline(date_override=yesterday); sys.exit(0)
  5. Else (normal live path): trader = NexusTrader(dry_run=False); scheduler = TradingScheduler(nexus_trader=trader); scheduler.start(); logger.info('nexus_trader running — press Ctrl+C to stop'); then shutdown_event.wait() in try/except KeyboardInterrupt that force-closes positions, saves state, shuts scheduler, sys.exit(0).

  if __name__ == '__main__': main()
  </action>

  <verify>
  <automated>python -c "import ast, sys; ast.parse(open('main.py').read()); print('Syntax OK')"</automated>
  </verify>

  <acceptance_criteria>
  - main.py parses without SyntaxError (ast.parse passes)
  - main.py contains the string "if args.dry_run:" followed by TradingScheduler NOT being instantiated in that branch
  - main.py contains "sys.exit(0)" inside the dry_run branch
  - main.py contains "NexusTrader(dry_run=False)" — not NexusTrader(dry_run=args.dry_run) or NexusTrader(dry_run=True)
  - main.py contains "run_pre_market_pipeline(date_override=yesterday)"
  - main.py contains "TradingScheduler" only inside the else/non-dry-run path
  - python -c "import main" does not execute main() (entry guard present: if __name__ == '__main__')
  - grep for "TradingScheduler" appears exactly once in main.py (inside non-dry-run path only)
  </acceptance_criteria>

  <done>python main.py --dry-run runs pre-market pipeline on yesterday's date and exits; python main.py starts the live scheduler path. No scheduler in dry-run branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → main() | User-controlled --dry-run flag; no untrusted data crosses this boundary |
| main.py → NexusTrader | date_override is computed internally from datetime.now() — not from user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | main.py dry-run branch | accept | date_override computed from datetime.now() minus 1 day — not user-controlled; no injection risk |
| T-06-02 | Denial of Service | sys.exit(0) in dry-run | accept | Intentional early exit; no resource leak since pipeline has its own cleanup |
| T-06-SC | Tampering | npm/pip installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- python main.py --help shows --dry-run flag with correct description
- python -c "import ast; ast.parse(open('main.py').read()); print('OK')" passes
- python -c "from execution.scheduler import NexusTrader; import inspect; assert 'date_override' in inspect.signature(NexusTrader.run_pre_market_pipeline).parameters" passes
- Grep confirms "TradingScheduler" appears only in the non-dry-run path of main.py
</verification>

<success_criteria>
- execution/scheduler.py: run_pre_market_pipeline(self, date_override: date | None = None) — default None falls back to datetime.now(ist).date()
- main.py: --dry-run branch calls NexusTrader(dry_run=False), run_pre_market_pipeline(date_override=yesterday), sys.exit(0) — no TradingScheduler instantiation
- main.py: non-dry-run path unchanged from Phase 5 spec
- Both files parse without SyntaxError
</success_criteria>

<output>
Create `.planning/phases/06-dry-run-backtest/06-01-SUMMARY.md` when done
</output>
