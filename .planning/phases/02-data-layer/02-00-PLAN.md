---
phase: "02-data-layer"
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_data_layer.py
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
  - DATA-06
  - DATA-07
  - DATA-08
  - DATA-09
  - DATA-10
  - DATA-11
  - DATA-12
  - DATA-13
  - DATA-14
  - DATA-15

must_haves:
  truths:
    - "tests/conftest.py exists with two fixtures: `fetcher` (MarketDataFetcher instance) and `sample_ohlcv` (deterministic 30-row 5-min DataFrame)"
    - "tests/test_data_layer.py exists with stub test functions for all 15 DATA-xx requirements"
    - "`pytest tests/test_data_layer.py -x -q` runs and all stubs fail with ImportError or pytest.fail() — confirming RED state"
    - "tests/__init__.py exists as empty file marking the package"
  artifacts:
    - path: "tests/__init__.py"
      provides: "Python package marker"
    - path: "tests/conftest.py"
      provides: "shared fixtures: fetcher, sample_ohlcv"
      exports: ["fetcher", "sample_ohlcv"]
    - path: "tests/test_data_layer.py"
      provides: "stub tests for DATA-01 through DATA-15"
      contains: "test_universe_count, test_universe_format, test_intraday_candles_bad_symbol, test_scalar_returns_none_on_failure, test_session_filter_09_15, test_global_indices_partial, test_get_atr_fetcher, test_vwap_no_nan, test_ema_output_shape, test_rsi_bounds, test_indicators_atr_returns_float, test_orb_tuple, test_volume_ratio_insufficient_data"
  key_links:
    - from: "tests/test_data_layer.py"
      to: "data/market_data.py"
      via: "from data.market_data import MarketDataFetcher"
      pattern: "from data\\.market_data import"
    - from: "tests/test_data_layer.py"
      to: "data/indicators.py"
      via: "from data.indicators import Indicators"
      pattern: "from data\\.indicators import"
    - from: "tests/test_data_layer.py"
      to: "data/universe.py"
      via: "from data.universe import get_nse_universe"
      pattern: "from data\\.universe import"
---

<objective>
Create the Wave 0 test scaffold: empty package marker, shared pytest fixtures, and stub test functions for all 15 DATA-xx requirements. All stubs must be in RED state (fail on collection or explicit pytest.fail) before Wave 1 implementation plans run.

Purpose: Nyquist compliance — every implementation task in Wave 1 has a corresponding failing test that it must make green. No test is created after the code it validates.
Output: tests/__init__.py, tests/conftest.py, tests/test_data_layer.py
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-data-layer/02-CONTEXT.md
@.planning/phases/02-data-layer/02-PATTERNS.md
@.planning/phases/02-data-layer/02-VALIDATION.md

<interfaces>
From config.py (line 57): `config = Config()` — singleton, accessible via `from config import config`
From utils/logger.py: `setup_logger(name: str) -> logging.Logger`

Data modules to be created in Wave 1 (not yet present — tests will import-fail until Wave 1 completes):
  from data.universe import get_nse_universe  # returns list[dict]
  from data.market_data import MarketDataFetcher  # class with fetch methods
  from data.indicators import Indicators  # class with @staticmethod methods
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/__init__.py and tests/conftest.py</name>
  <files>tests/__init__.py, tests/conftest.py</files>
  <read_first>
    - .planning/phases/02-data-layer/02-PATTERNS.md (conftest.py pattern in "tests/conftest.py" section — the full fixture code is the reference)
    - .planning/phases/02-data-layer/02-VALIDATION.md (Wave 0 Requirements section)
  </read_first>
  <action>
    Create tests/__init__.py as a single blank line (empty package marker — per the existing agents/__init__.py and data/__init__.py convention).

    Create tests/conftest.py with these two fixtures exactly:

    Fixture 1 — `fetcher` (scope="module"): Returns a MarketDataFetcher() instance. Import: `from data.market_data import MarketDataFetcher`. This fixture will ImportError until Wave 1 completes — that is correct and expected.

    Fixture 2 — `sample_ohlcv` (scope="function"): Returns a deterministic 30-row DataFrame with no network calls. Build it as follows:
    - Seed numpy with 42: `np.random.seed(42)`
    - Base price: 2500.0
    - closes = 2500.0 + cumsum(randn(30) * 10)
    - highs = closes + uniform(5, 20, 30)
    - lows = closes - uniform(5, 20, 30)
    - opens = closes + randn(30) * 5
    - volumes = randint(10_000, 200_000, 30).astype(float)
    - index = pd.date_range("2026-06-06 09:15", periods=30, freq="5min", tz="Asia/Kolkata")
    - Return pd.DataFrame({"Open": opens, "High": highs, "Low": lows, "Close": closes, "Volume": volumes}, index=index)

    Required imports in conftest.py: pytest, pandas as pd, numpy as np, and the MarketDataFetcher import (which will fail until Wave 1 — acceptable).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('tests/conftest.py').read()); print('conftest.py syntax OK')"</automated>
  </verify>
  <acceptance_criteria>
    - tests/__init__.py exists (empty or single blank line)
    - tests/conftest.py exists with no syntax errors
    - tests/conftest.py contains `@pytest.fixture` decorator for both `fetcher` and `sample_ohlcv`
    - `sample_ohlcv` fixture builds DataFrame with exactly 30 rows and columns Open, High, Low, Close, Volume
    - `sample_ohlcv` fixture does NOT make any network calls (pure numpy/pandas construction)
    - `fetcher` fixture imports from `data.market_data` (the import may fail until Wave 1 — this is correct RED behavior)
  </acceptance_criteria>
  <done>tests/__init__.py and tests/conftest.py written and syntactically valid</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/test_data_layer.py with all DATA-xx stub tests</name>
  <files>tests/test_data_layer.py</files>
  <read_first>
    - .planning/phases/02-data-layer/02-VALIDATION.md (Per-Task Verification Map — maps each DATA-xx to its test function name)
    - .planning/phases/02-data-layer/02-PATTERNS.md (test_data_layer.py pattern section — structure reference)
    - .planning/REQUIREMENTS.md (DATA-01 through DATA-15 — what each test must validate)
  </read_first>
  <action>
    Create tests/test_data_layer.py. Organize tests into three pytest classes: TestUniverse, TestMarketDataFetcher, TestIndicators. All tests start as stubs — they either raise ImportError on collection (because data modules do not exist yet) or contain `pytest.fail("not implemented yet")` as the body.

    Required test functions (exact names must match VALIDATION.md Per-Task Verification Map):

    Class TestUniverse:
    - test_universe_count: asserts len(get_nse_universe()) == 100
    - test_universe_format: asserts all symbols end in ".NS" and all dicts have "sector" key
    - test_universe_no_duplicates: asserts len(set(s["symbol"] for s in u)) == 100

    Class TestMarketDataFetcher (uses `fetcher` fixture):
    - test_intraday_candles_bad_symbol: calls fetcher.get_intraday_candles("INVALID_XYZ_999.NS"), asserts result is empty DataFrame (df.empty == True) without raising
    - test_prepost_false: stub verifying prepost=False is enforced — body: `pytest.fail("not implemented yet")` (verified by code inspection in Wave 1)
    - test_rate_limit_delay: stub verifying 0.2s sleep — body: `pytest.fail("not implemented yet")` (verified by code inspection in Wave 1)
    - test_scalar_returns_none_on_failure: calls fetcher.get_previous_close("INVALID_XYZ_999.NS"), asserts result is None
    - test_session_filter_09_15: stub that calls fetcher.get_intraday_candles("RELIANCE.NS") and verifies all index timestamps have hour >= 9 and minute >= 15 when hour == 9; body: `pytest.fail("not implemented yet")` (live network — manual verify only per VALIDATION.md)
    - test_global_indices_partial: uses monkeypatch to raise on first yf.Ticker call, asserts result is a dict (not an exception)
    - test_get_atr_fetcher: calls fetcher.get_atr("INVALID_XYZ_999.NS"), asserts result is None without raising

    Class TestIndicators (uses `sample_ohlcv` fixture):
    - test_vwap_no_nan: calls Indicators.vwap(sample_ohlcv), asserts isinstance(result, pd.Series) and not result.isna().any()
    - test_ema_output_shape: calls Indicators.ema(sample_ohlcv, period=9), asserts len(result) == len(sample_ohlcv)
    - test_rsi_bounds: calls Indicators.rsi(sample_ohlcv), asserts valid = result.dropna(); (valid >= 0).all() and (valid <= 100).all()
    - test_indicators_atr_returns_float: calls Indicators.atr(sample_ohlcv), asserts isinstance(result, float) and not pd.isna(result)
    - test_orb_tuple: calls Indicators.orb(sample_ohlcv, n_minutes=15), asserts high >= low
    - test_volume_ratio_insufficient_data: builds a 1-row DataFrame, calls Indicators.volume_ratio(df), asserts result == 0.0

    Required imports at top of file: pytest, pandas as pd, numpy as np, and the three data module imports (all will ImportError on collection until Wave 1 — this is the expected RED state).

    Do NOT use fenced code blocks in any test body. Tests must be runnable pytest functions.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('tests/test_data_layer.py').read()); print('test_data_layer.py syntax OK')"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_data_layer.py exists with no syntax errors
    - File contains classes TestUniverse, TestMarketDataFetcher, TestIndicators
    - Total test function count >= 15 (one per DATA-xx requirement)
    - All test function names exactly match those listed in VALIDATION.md Per-Task Verification Map
    - Stub tests either ImportError on collection (acceptable RED) or contain `pytest.fail("not implemented yet")` as the body
    - No test body imports or calls `ta` library
    - `python -c "import ast; ast.parse(open('tests/test_data_layer.py').read())"` exits 0
  </acceptance_criteria>
  <done>tests/test_data_layer.py written with >= 15 stub tests, all syntactically valid, confirmed RED state when data modules absent</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → production modules | Test files import from data.* — no untrusted input, but import failures must be expected and tolerated in RED state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-00-01 | Information Disclosure | tests/conftest.py | accept | No secrets in test fixtures; sample_ohlcv is synthetic data only; fetcher fixture imports from production code but does not expose API keys in test files |
| T-02-00-SC | Tampering | No package installs in this plan | accept | No npm/pip/cargo installs — only file creation; no slopcheck required |
</threat_model>

<verification>
After Wave 0 completes, verify RED state:

```
# Syntax check both files
python -c "import ast; ast.parse(open('tests/conftest.py').read()); print('OK')"
python -c "import ast; ast.parse(open('tests/test_data_layer.py').read()); print('OK')"

# Confirm RED state — should see ImportError or collection errors because data modules don't exist
pytest tests/test_data_layer.py --collect-only 2>&1 | head -20
```

Expected: collection errors referencing missing data.market_data, data.indicators, data.universe. This is correct — Wave 1 plans will make them green.
</verification>

<success_criteria>
- tests/__init__.py, tests/conftest.py, tests/test_data_layer.py all exist
- Both .py files pass AST parse (no syntax errors)
- Test file contains >= 15 test functions spanning TestUniverse, TestMarketDataFetcher, TestIndicators
- All test names match VALIDATION.md Per-Task Verification Map exactly
- No `ta` library references in any test file
- Running pytest against these files produces collection errors (RED) — not a passing suite
</success_criteria>

<output>
Create `.planning/phases/02-data-layer/02-00-SUMMARY.md` when done
</output>
